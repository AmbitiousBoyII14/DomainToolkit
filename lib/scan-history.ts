const KEY = 'dtp_domain_history'
export function getDomainHistory(): string[] { if (typeof window === 'undefined') return []; try { return JSON.parse(localStorage.getItem(KEY) ?? '[]') } catch { return [] } }
export function addDomainToHistory(domain: string): void { if (typeof window === 'undefined' || !domain.trim()) return; const h = getDomainHistory().filter(d => d !== domain); h.unshift(domain); localStorage.setItem(KEY, JSON.stringify(h.slice(0, 50))) }
export function suggestDomains(query: string): string[] { if (!query.trim()) return getDomainHistory().slice(0, 5); const q = query.toLowerCase(); return getDomainHistory().filter(d => d.toLowerCase().includes(q)).slice(0, 5) }