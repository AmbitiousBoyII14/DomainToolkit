'use client'
import { useState, useEffect } from 'react'
import { X, Eye, EyeOff, Mail, User, Lock, ArrowLeft, CheckCircle, KeyRound, Sparkles } from 'lucide-react'
import { signUpEmail, signInEmail, forgotPassword, resetPassword, type User as AuthUser } from '@/lib/auth'

interface AuthModalProps { isOpen: boolean; initialMode?: 'signin' | 'signup'; onClose: () => void; onSuccess: (user: AuthUser) => void }
type FlowMode = 'signin' | 'signup' | 'forgot' | 'forgot-sent' | 'reset'

export function AuthModal({ isOpen, initialMode = 'signup', onClose, onSuccess }: AuthModalProps) {
  const [mode, setMode] = useState<FlowMode>(initialMode)
  const [name, setName] = useState(''); const [email, setEmail] = useState('')
  const [password, setPassword] = useState(''); const [showPass, setShowPass] = useState(false)
  const [error, setError] = useState(''); const [successMsg, setSuccessMsg] = useState('')
  const [loading, setLoading] = useState(false)
  const [resetUrl, setResetUrl] = useState(''); const [resetToken, setResetToken] = useState(''); const [resetEmail, setResetEmail] = useState('')

  useEffect(() => { if (typeof window !== 'undefined') { const p = new URLSearchParams(window.location.search); const t = p.get('token'); const e = p.get('email'); if (t && e) { setResetToken(t); setResetEmail(decodeURIComponent(e)); setMode('reset') } } }, [isOpen])
  useEffect(() => { if (isOpen) { setMode(initialMode); setError(''); setSuccessMsg(''); setName(''); setEmail(''); setPassword(''); setShowPass(false); setResetUrl('') } }, [isOpen, initialMode])
  if (!isOpen) return null

  function handleSubmit(e: React.FormEvent) { e.preventDefault(); setError(''); if (!email.includes('@')) { setError('Please enter a valid email.'); return }; if (password.length < 6) { setError('Password must be at least 6 characters.'); return }; setLoading(true); setTimeout(() => { const r = mode === 'signup' ? signUpEmail(name, email, password) : signInEmail(email, password); setLoading(false); if (r.error) { setError(r.error); return }; onSuccess(r.user!); onClose() }, 600) }
  async function handleForgot(e: React.FormEvent) { e.preventDefault(); setError(''); setSuccessMsg(''); if (!email.includes('@')) { setError('Please enter a valid email.'); return }; setLoading(true); const r = await forgotPassword(email); setLoading(false); if (r.success) { if (r.resetUrl) setResetUrl(r.resetUrl); setSuccessMsg(r.message); setMode('forgot-sent') } else setError(r.message) }
  async function handleReset(e: React.FormEvent) { e.preventDefault(); setError(''); setSuccessMsg(''); if (password.length < 6) { setError('Password must be at least 6 characters.'); return }; setLoading(true); const r = await resetPassword(resetToken, resetEmail, password); setLoading(false); if (r.success) { setSuccessMsg(r.message + ' Redirecting...'); setTimeout(() => { setMode('signin'); setSuccessMsg(''); setEmail(resetEmail); setPassword('') }, 2000) } else setError(r.message) }

  const titles: Record<FlowMode, string> = { signup: 'Create account', signin: 'Welcome back', forgot: 'Reset password', 'forgot-sent': 'Check inbox', reset: 'New password' }
  const subs: Record<FlowMode, string> = { signup: 'Start with 5 free scans/day', signin: 'Sign in to continue', forgot: "We'll send a reset link", 'forgot-sent': 'Follow the link sent to your email', reset: 'Choose a password for ' + resetEmail }

  return (
    <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center p-0 sm:p-4" aria-modal="true" role="dialog">
      <div className="absolute inset-0 bg-black/70 backdrop-blur-md" onClick={onClose} />
      <div className="relative z-10 w-full sm:max-w-md bg-card/90 backdrop-blur-xl border border-border rounded-t-2xl sm:rounded-2xl shadow-2xl overflow-hidden">
        <div className="flex items-center justify-between px-6 pt-5 pb-4 border-b border-border"><div className="flex items-center gap-3">{(mode === 'forgot' || mode === 'forgot-sent' || mode === 'reset') && (<button onClick={() => { setMode('signin'); setError(''); setSuccessMsg('') }} className="p-1.5 rounded-lg hover:bg-secondary text-muted-foreground"><ArrowLeft size={16} /></button>)}<div><h2 className="font-bold text-lg text-foreground flex items-center gap-2">{titles[mode]}{mode === 'signup' && <Sparkles size={15} className="text-primary" />}</h2><p className="text-sm text-muted-foreground">{subs[mode]}</p></div></div><button onClick={onClose} className="p-2 rounded-xl hover:bg-secondary text-muted-foreground"><X size={18} /></button></div>
        {successMsg && <div className="mx-6 mt-4 p-3 rounded-xl bg-success/10 border border-success/20 text-success text-sm flex gap-2"><CheckCircle size={16} className="shrink-0 mt-0.5" /><span>{successMsg}</span></div>}
        {error && <div className="mx-6 mt-4 p-3 rounded-xl bg-destructive/5 border border-destructive/20 text-destructive text-sm">{error}</div>}
        {mode === 'reset' ? (<form onSubmit={handleReset} className="px-6 py-5 space-y-4"><div><label className="block text-[11px] font-semibold text-muted-foreground uppercase tracking-wider mb-2">New Password</label><div className="relative"><KeyRound size={15} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground" /><input type={showPass ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} placeholder="Min. 6 characters" required className="w-full rounded-xl border border-input bg-secondary/50 pl-10 pr-10 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-primary/30 transition-all" /><button type="button" onClick={() => setShowPass(s => !s)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground">{showPass ? <EyeOff size={15} /> : <Eye size={15} />}</button></div></div><button type="submit" disabled={loading} className="w-full rounded-xl bg-gradient-to-r from-cyan to-accent text-white py-3 text-sm font-semibold hover:shadow-[0_0_25px_rgba(124,58,237,0.2)] disabled:opacity-50 transition-all">{loading ? 'Resetting...' : 'Reset Password'}</button></form>) : mode === 'forgot' ? (<form onSubmit={handleForgot} className="px-6 py-5 space-y-4"><div><label className="block text-[11px] font-semibold text-muted-foreground uppercase tracking-wider mb-2">Email</label><div className="relative"><Mail size={15} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground" /><input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@example.com" required className="w-full rounded-xl border border-input bg-secondary/50 pl-10 pr-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-primary/30 transition-all" /></div></div><button type="submit" disabled={loading} className="w-full rounded-xl bg-gradient-to-r from-cyan to-accent text-white py-3 text-sm font-semibold hover:shadow-[0_0_25px_rgba(124,58,237,0.2)] disabled:opacity-50 transition-all">{loading ? 'Sending...' : 'Send Reset Link'}</button></form>) : mode === 'forgot-sent' ? (<div className="px-6 py-8 text-center space-y-4"><div className="w-16 h-16 rounded-2xl bg-success/10 flex items-center justify-center mx-auto"><Mail size={24} className="text-success" /></div><p className="text-sm text-muted-foreground">Reset link sent to <strong className="text-foreground">{email}</strong>.</p>{resetUrl && <div className="p-3 rounded-xl bg-secondary text-xs text-muted-foreground break-all font-mono"><p className="mb-1 font-semibold text-foreground">🔑 Reset URL:</p>{resetUrl}</div>}<button onClick={() => { setMode('signin'); setError(''); setSuccessMsg('') }} className="text-sm text-primary font-semibold hover:underline">Back to sign in</button></div>) : (<form onSubmit={handleSubmit} className="px-6 py-5 space-y-4">{mode === 'signup' && (<div><label className="block text-[11px] font-semibold text-muted-foreground uppercase tracking-wider mb-2">Full Name</label><div className="relative"><User size={15} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground" /><input type="text" value={name} onChange={e => setName(e.target.value)} placeholder="John Doe" className="w-full rounded-xl border border-input bg-secondary/50 pl-10 pr-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-primary/30 transition-all" /></div></div>)}<div><label className="block text-[11px] font-semibold text-muted-foreground uppercase tracking-wider mb-2">Email</label><div className="relative"><Mail size={15} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground" /><input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@example.com" required className="w-full rounded-xl border border-input bg-secondary/50 pl-10 pr-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-primary/30 transition-all" /></div></div><div><label className="block text-[11px] font-semibold text-muted-foreground uppercase tracking-wider mb-2">Password</label><div className="relative"><Lock size={15} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground" /><input type={showPass ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} placeholder="Min. 6 characters" required className="w-full rounded-xl border border-input bg-secondary/50 pl-10 pr-10 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-primary/30 transition-all" /><button type="button" onClick={() => setShowPass(s => !s)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground">{showPass ? <EyeOff size={15} /> : <Eye size={15} />}</button></div></div><button type="submit" disabled={loading} className="w-full rounded-xl bg-gradient-to-r from-cyan to-accent text-white py-3.5 text-sm font-semibold hover:shadow-[0_0_25px_rgba(124,58,237,0.2)] disabled:opacity-50 transition-all mt-2">{loading ? (mode === 'signup' ? 'Creating...' : 'Signing in...') : (mode === 'signup' ? 'Create free account' : 'Sign in')}</button><div className="flex items-center justify-between pt-1"><p className="text-sm text-muted-foreground">{mode === 'signup' ? 'Already have an account? ' : "Don't have an account? "}<button type="button" onClick={() => { setMode(mode === 'signup' ? 'signin' : 'signup'); setError('') }} className="text-primary font-semibold hover:underline">{mode === 'signup' ? 'Sign in' : 'Sign up'}</button></p>{mode === 'signin' && <button type="button" onClick={() => { setMode('forgot'); setError(''); setSuccessMsg('') }} className="text-xs text-muted-foreground hover:text-primary">Forgot password?</button>}</div></form>)}</div></div>) }